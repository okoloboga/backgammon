# Этап 1: Завершение игрового процесса (на моках)

## 🎯 Цель
Полностью рабочая игра в длинные нарды БЕЗ реальных транзакций. Игроки могут создавать комнаты, играть, видеть победителя. Mock-режим для всех денежных операций.

---

## ⚠️ Текущая проблема

**Статус**: Решается (см. ISSUE.md)

**Суть**: При переходе из MainMenu в GameRoom возникают ошибки:
- Race condition с async cleanup в RoomList
- React StrictMode вызывает преждевременный disconnect
- Несинхронизированные форматы данных между сервером и клиентом

**Решение в процессе**: Исправление async/await логики, защита от StrictMode, унификация форматов данных.

---

## 📋 Задачи

### 1.1 Исправление перехода в игру

**Что делаем**:
- Завершить исправления из ISSUE.md
- Проверить стабильность перехода MainMenu → GameRoom
- Убедиться, что состояние игры синхронизируется корректно
- Проверить, что оба игрока видят одинаковую доску

**Критерий**: Создали комнату → присоединились → оба видят игровую доску с начальной позицией

---

### 1.2 Mock-режим для транзакций

**Что делаем**:
- Флаг `USE_MOCK_TRANSACTIONS=true` (в .env)
- При создании комнаты:
  - Frontend НЕ вызывает TonConnect
  - Backend НЕ вызывает контракт
  - Генерируется mock gameId
  - Комната создается сразу в БД
- При присоединении - аналогично
- При завершении игры:
  - Backend НЕ вызывает ReportWinner
  - Просто обновляем статус комнаты в БД
  - Показываем победителя

**Критерий**: Можно играть без подключения кошелька, без реальных транзакций

---

### 1.3 Интерактивный GameRoom UI

**Что делаем**:

**Бросок костей**:
- Кнопка "Roll Dice" активна для текущего игрока
- Анимация броска
- Показ выпавших значений

**Выбор и перемещение шашек**:
- Подсветка шашек, которыми можно ходить
- Клик на шашку → подсветка возможных пунктов назначения
- Клик на подсвеченный пункт → отправка хода на сервер
- Оптимистичное обновление UI
- Анимация перемещения шашки

**Информация**:
- Индикатор текущего игрока ("Your turn" / "Opponent's turn")
- Оставшиеся ходы (сколько костей еще не использовано)
- Снятые шашки
- Шашки на баре

**Обработка ошибок**:
- Тосты при невалидном ходе
- Сообщения от сервера

**Критерий**: Можно кликать на шашки, видеть доступные ходы, делать ходы, видеть анимации

---

### 1.4 Таймер на ход

**Backend (BackgammonRoom)**:
- При начале хода запускать таймер 60 секунд
- Каждую секунду обновлять `state.turnTimeLeft`
- При достижении 0:
  - Если у игрока были возможные ходы → проигрыш
  - Передать ход оппоненту или завершить игру
- За 10 секунд до конца → предупреждение клиенту

**Frontend (GameRoom)**:
- Отображать обратный отсчет
- За 10 секунд - красная подсветка
- Звук/вибрация (опционально)

**Критерий**: Таймер работает, при таймауте игрок проигрывает

---

### 1.5 Обработка отключений

**Backend**:
- При отключении игрока:
  - Если игра не началась (waiting) → удалить комнату
  - Если игра идет (in_progress):
    - Таймер ожидания 30 секунд
    - Если не вернулся → проигрыш отключившемуся
- При переподключении:
  - Восстановить состояние игры
  - Продолжить с текущего момента

**Frontend**:
- Индикатор "Opponent disconnected, waiting..."
- Автопопытка переподключения

**Критерий**: Отключение одного игрока не ломает игру для другого

---

### 1.6 Завершение игры

**Backend (BackgammonRoom)**:
- При снятии 15 шашек одним игроком:
  - `state.winner = winnerAddress`
  - `state.phase = 'finished'`
  - Обновить БД (state = 'finished', winner_address)
  - Отправить клиентам `game_finished`:
    - winner, loser
    - reason (normal_win, mars, timeout, disconnect)
  - Обновить статистику игроков в таблице `users`
  - Сохранить в таблицу `game_history`
  - Заблокировать комнату
  - Удалить через 30 секунд

**Таблица `game_history`**:
- id, game_id, room_id
- player1_address, player2_address
- winner_address, loser_address
- currency, amount (mock)
- duration_seconds, moves_count
- finish_reason
- finished_at

**Миграция**: TypeORM

**Критерий**: Игра завершается корректно, победитель определяется, данные сохраняются

---

### 1.7 Экран завершения игры

**Frontend (GameFinished компонент)**:
- Анимация победы/поражения
- "You Win!" / "You Lose"
- Причина завершения
- Статистика партии (длительность, ходы)
- Кнопки:
  - "Back to Menu"
  - "Play Again" (создать новую комнату)

**Критерий**: После завершения игры показывается красивый экран с результатом

---

### 1.8 Анимации и UX

**Добавить**:
- Плавное перемещение шашек (CSS transitions)
- Анимация броска костей
- Подсветка доступных ходов (glow)
- Пульсация текущего игрока
- Тосты для уведомлений
- Скелетоны при загрузке
- Индикаторы загрузки

**Критерий**: Интерфейс выглядит живым и отзывчивым

---

### 1.9 База данных

**Таблица `rooms` (для mock-режима)**:
- id (UUID)
- game_id (BIGINT, mock) - генерируется на backend
- creator_address
- joiner_address
- currency (TON/RUBLE)
- amount
- state (waiting, in_progress, finished, cancelled)
- created_at, joined_at
- winner_address
- colyseus_room_id

**Таблица `game_history`**:
- См. п. 1.6

**Миграции**: TypeORM

**Критерий**: Все комнаты и партии сохраняются в БД

---

### 1.10 Тестирование

**Сценарии**:
- Создание комнаты → присоединение → полная партия → завершение
- Таймаут хода → проигрыш
- Отключение игрока → проигрыш
- Создание комнаты → никто не присоединился → отмена
- Два игрока создают комнаты одновременно

**Тестирование**:
- Desktop (Chrome, Firefox, Safari)
- Mobile (iOS, Android)
- Telegram WebApp (все платформы)

**Критерий**: Все сценарии работают стабильно

---

## ✅ Критерии завершения этапа

- ✅ Проблема перехода в игру решена
- ✅ Mock-режим работает (можно играть без кошелька)
- ✅ Полное UI взаимодействие (клики, ходы, анимации)
- ✅ Таймер работает, таймаут обрабатывается
- ✅ Отключение игрока обрабатывается
- ✅ Победитель определяется автоматически
- ✅ Экран завершения показывает результат
- ✅ История партий сохраняется в БД
- ✅ Протестированы все основные сценарии

---

## 🎯 Результат

После завершения этапа 1:
- Пользователи могут полноценно играть в нарды
- Все работает без реальных транзакций (mock)
- Убедились, что логика игры корректна
- UI интерактивен и приятен
- Готовы к интеграции реальных денег (Этап 2)

---

**Следующий шаг**: S2.md - Интеграция смартконтракта

