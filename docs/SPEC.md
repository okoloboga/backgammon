# 🎯 ROADMAP: Backgammon Online - TON Blockchain Game

## 📋 Общая информация о проекте

**Цель**: Запустить полнофункциональную онлайн-игру в длинные нарды со ставками в TON и RUBLE токенах.

**Текущее состояние проекта** (актуализировано):

### ✅ Полностью реализовано

#### Frontend (React + Vite)
- ✅ **SplashScreen**: Логотип + кнопка подключения TonConnect
- ✅ **Авторизация**: TonConnect интеграция работает полностью
- ✅ **MainMenu**: 
  - Отображение профиля пользователя (имя, аватар из TON кошелька)
  - Отображение балансов TON и RUBLE из кошелька (через TonCenter API v2/v3)
  - Список активных игровых комнат (RoomList)
  - Кнопка создания комнаты
- ✅ **CreateRoomModal**: 
  - Выбор валюты (TON/RUBLE)
  - Ввод суммы ставки
  - Валидация минимальной ставки (TON >= 1.0, RUBLE >= 1000)
  - Валидация баланса пользователя
  - Создание комнаты через backend API
- ✅ **RoomList**: Подписка на Colyseus Lobby, динамическое обновление списка комнат
- ✅ **GameRoom**: 
  - Базовая структура компонента
  - Подключение к Colyseus Room
  - Отображение игровой доски (24 пункта)
  - Компоненты: BoardPoint, Checker, Dice, PlayerProfile
  - Обработка onStateChange от сервера

#### Backend (NestJS)
- ✅ **Users Module**:
  - PostgreSQL + TypeORM интеграция
  - Таблица `users` с полями: id, walletAddress, username, avatar, games, wins, loses, winrate, streaks, level, experience, friends, achievements
  - CRUD операции для пользователей
  - Обновление статистики игроков
- ✅ **Auth Module**:
  - JWT авторизация
  - TonProof verification через ChallengeService
  - Генерация и верификация challenge
  - Создание/обновление пользователей при авторизации
  - Парсинг Telegram initData для получения username/avatar
- ✅ **TON Module**:
  - TonClient инициализация
  - Получение баланса кошелька
  - Заглушки для транзакций (пока mock)
  - Интеграция с TonCenter API
- ✅ **Game Module**:
  - GameController с эндпоинтом `/game-http/create_room`
  - GameService для инициализации Colyseus Server
  - WebSocket транспорт настроен
- ✅ **Config Module**: Централизованная конфигурация (порт, TON endpoint, database, JWT)

#### Game Logic (Colyseus)
- ✅ **BackgammonRoom**:
  - Полная реализация игровой логики длинных нард
  - GameState Schema с синхронизацией клиента
  - Начальная расстановка шашек (белые на 24, черные на 1)
  - Обработка броска костей (включая дубли - 4 хода)
  - Расчет всех возможных ходов с учетом правил:
    - Обязательность использования всех ходов
    - Приоритет больших ходов
    - Невозможность ставить на пункт противника
    - Съедание одиночных шашек (blot)
    - Логика снятия шашек (bearing off)
  - Валидация ходов на сервере
  - Определение победителя (15 шашек снято)
  - Обработка подключения/отключения игроков
  - Автоматическое завершение хода при отсутствии возможных ходов
- ✅ **GameState Schema**:
  - `board`: Map<pointId, Point> - состояние 24 пунктов
  - `bar`: Map<color, count> - сбитые шашки
  - `off`: Map<color, count> - снятые шашки
  - `currentPlayer`: string - текущий игрок
  - `dice`: number[] - значения костей
  - `winner`: string - победитель
  - `possibleMoves`: string[] - доступные ходы
  - `players`: Map<sessionId, color> - игроки в комнате
- ✅ **LobbyRoom**: Отслеживание списка активных комнат для frontend
- ✅ **LobbyService**: Управление информацией о комнатах

#### Blockchain
- ✅ **Escrow Smart Contract** (Tact):
  - Написан и скомпилирован
  - Поддержка TON и Jetton (RUBLE)
  - Структуры: Game, JettonPayload
  - Сообщения: CreateGameTon, JoinGameTon, CancelGame, ReportWinner, WithdrawUnclaimed, OnJettonTransfer, ClaimJetton
  - Логика создания игры, присоединения, отмены
  - Автоматическая выплата для TON (5% комиссия)
  - Pull-model (ClaimJetton) для Jetton
  - **Статус**: НЕ задеплоен в сеть

### ⚠️ Частично реализовано

- ⚠️ **GameRoom UI**: Структура есть, но нет полного взаимодействия с игрой (отправка ходов, анимации, завершение игры)
- ⚠️ **Переход в игру**: Есть проблема с подключением к созданной комнате после CreateRoomModal

### ❌ НЕ реализовано

#### Интеграция смарт-контракта
- ❌ Деплой Escrow контракта в TON
- ❌ EscrowService на backend для взаимодействия с контрактом
- ❌ Методы вызова контракта: deployContract, createGameTon, createGameJetton, joinGameTon, joinGameJetton, reportWinner
- ❌ Мониторинг событий контракта (ContractMonitorService)
- ❌ Верификация транзакций
- ❌ Интеграция с TonConnect для отправки транзакций с клиента

#### База данных
- ❌ Таблица `rooms` для хранения игровых комнат с привязкой к контракту
- ❌ Таблица `game_history` для истории игр
- ❌ Таблица `blockchain_events` для логирования транзакций
- ❌ Миграции БД

#### Игровой процесс
- ❌ Таймер на ход (60 секунд)
- ❌ Автопроигрыш при таймауте
- ❌ Reconnect логика при разрыве соединения
- ❌ Обработка отключения игрока
- ❌ Завершение игры с вызовом ReportWinner в контракте
- ❌ Экран завершения игры (GameFinished)
- ❌ Кнопка "Забрать выигрыш" для RUBLE игр
- ❌ Реванш

#### Статистика и профиль
- ❌ Страница профиля игрока
- ❌ История игр пользователя
- ❌ Leaderboard (опционально)
- ❌ Автоматическое обновление статистики при завершении игры

#### UX и производительность
- ❌ Анимации перемещения шашек
- ❌ Подсветка доступных ходов
- ❌ Индикатор текущего игрока
- ❌ Тосты для уведомлений
- ❌ Скелетоны при загрузке
- ❌ Оптимистичные обновления UI
- ❌ Обработка ошибок с понятными сообщениями

#### Деплой и инфраструктура
- ❌ Production деплой backend
- ❌ Production деплой frontend
- ❌ Docker configuration
- ❌ Nginx reverse proxy
- ❌ SSL сертификаты
- ❌ Мониторинг и логирование (Winston, Sentry)
- ❌ Telegram Mini App настройка

#### Безопасность
- ❌ Rate limiting
- ❌ CORS настройки для production
- ❌ Аудит смарт-контракта

#### Документация
- ❌ API документация (полная)
- ❌ Deployment инструкции
- ❌ FAQ для пользователей

---

**Технологии**: 
- Frontend: React, Vite, TonConnect UI
- Backend: NestJS, TypeScript, TON SDK
- Blockchain: TON (The Open Network), Tact
- Real-time: Colyseus (WebSocket)
- Database: PostgreSQL, TypeORM
- Game Logic: Colyseus Schema

---

## 🚀 ЭТАП 1: Интеграция смарт-контракта и завершение базового функционала

### Цель этапа
Обеспечить полный цикл создания комнаты с реальной блокчейн-транзакцией и корректным переходом в игру. Исправить существующую ошибку перехода от создания комнаты к игре.

### Задачи

#### 1.1 Исправление текущей ошибки перехода в игру

**Проблема**: При создании комнаты через CreateRoomModal возникает ошибка при попытке подключиться к GameRoom.

**Решение**:
- Проверить логику сохранения roomId после создания комнаты
- Убедиться, что GameRoom получает корректный roomId
- Проверить, что Colyseus Room существует и доступна для подключения
- Отладить синхронизацию состояния между клиентом и сервером
- Убедиться, что onStateChange триггерится корректно

#### 1.2 Backend: Деплой и интеграция Escrow контракта

**Создать**: `EscrowService` в модуле `ton/`

**Методы**:
- `deployContract()` - деплой Escrow контракта в TON testnet/mainnet
- `getContractAddress()` - получить адрес задеплоенного контракта
- `createGameTon(walletAddress, amount, joinTimeout)` - создание TON игры (0x01)
- `createGameJetton(walletAddress, jettonMaster, amount, payload)` - создание Jetton игры через OnJettonTransfer
- `joinGameTon(walletAddress, gameId, amount)` - присоединение к TON игре (0x02)
- `joinGameJetton(walletAddress, gameId, jettonMaster, amount)` - присоединение к Jetton игре
- `reportWinner(gameId, winnerAddress)` - вызов ReportWinner (0x04) от админа
- `cancelGame(gameId)` - отмена игры (0x03)
- `withdrawUnclaimed()` - вывод неиспользованных TON (0x05)
- `getGameState(gameId)` - получить состояние игры из контракта
- `listenToContractEvents()` - фоновый мониторинг событий контракта

**Конфигурация** (добавить в `.env`):
- `TON_ESCROW_ADDRESS` - адрес контракта
- `TON_ADMIN_MNEMONIC` - мнемоника админского кошелька
- `RUBLE_JETTON_MASTER` - адрес RUBLE токена
- `MIN_TON_BET` - минимальная ставка в TON (по умолчанию 1)
- `MIN_RUBLE_BET` - минимальная ставка в RUBLE (по умолчанию 1000)
- `TON_FEE_PERCENT` - процент комиссии для TON (5%)

**Валидация**: Проверка минимальных ставок на уровне API

#### 1.3 Backend: Создание модуля управления комнатами (RoomsModule)

**Создать**: `RoomsModule` с `RoomsService` и `RoomsController`

**Эндпоинты**:
- `POST /rooms/create` - создание комнаты:
  - Принимает: currency (TON/RUBLE), amount, walletAddress
  - Валидирует баланс пользователя (через TonService)
  - Вызывает EscrowService для создания игры в контракте
  - Сохраняет в БД таблицу `rooms`
  - Возвращает: roomId, gameId (из контракта), txHash
- `POST /rooms/:roomId/join` - присоединение к комнате:
  - Принимает: walletAddress
  - Вызывает EscrowService для присоединения к игре
  - Обновляет статус комнаты в БД
  - Инициирует создание Colyseus BackgammonRoom
  - Возвращает: roomId, sessionId
- `GET /rooms/available` - список доступных комнат (waiting)
- `GET /rooms/:roomId` - детали конкретной комнаты
- `DELETE /rooms/:roomId/cancel` - отмена комнаты создателем

**Таблица БД** `rooms`:
- `id` (UUID, PK) - внутренний ID комнаты
- `game_id` (BIGINT, UNIQUE) - ID игры из смарт-контракта
- `creator_address` (VARCHAR) - адрес создателя
- `joiner_address` (VARCHAR, nullable) - адрес присоединившегося игрока
- `currency` (VARCHAR) - TON или RUBLE
- `amount` (DECIMAL) - сумма ставки
- `state` (VARCHAR) - waiting, in_progress, finished, cancelled
- `created_at` (TIMESTAMP)
- `joined_at` (TIMESTAMP, nullable)
- `winner_address` (VARCHAR, nullable)
- `winner_tx_hash` (VARCHAR, nullable) - хеш транзакции выплаты
- `tx_hash` (VARCHAR) - хеш транзакции создания
- `colyseus_room_id` (VARCHAR, nullable) - ID Colyseus комнаты

**Миграция**: Создать миграцию TypeORM для таблицы `rooms`

#### 1.4 Frontend: TonTransactionService для работы с транзакциями

**Создать**: `services/tonTransactionService.js`

**Методы**:
- `createGameTon(amount)` - отправка транзакции CreateGameTon через TonConnect
- `createGameRuble(amount)` - отправка Jetton transfer с payload action=0
- `joinGameTon(gameId, amount)` - отправка транзакции JoinGameTon
- `joinGameRuble(gameId, amount)` - отправка Jetton transfer с payload action=1
- `claimJettonWinnings(gameId)` - вызов ClaimJetton для получения выигрыша
- `waitForConfirmation(txHash)` - polling транзакции до подтверждения
- `buildJettonPayload(action, gameId)` - построение payload для Jetton transfer

**Интеграция с TonConnect**: 
- Использовать `tonConnectUI.sendTransaction()` для отправки транзакций
- Обработка успеха/ошибки
- Отображение статуса транзакции пользователю

#### 1.5 Frontend: Обновление CreateRoomModal

**Изменения**:
- При нажатии "Create":
  1. Показать индикатор загрузки
  2. Вызвать TonTransactionService для создания транзакции
  3. Дождаться подтверждения транзакции (или показать статус "pending")
  4. Вызвать `POST /rooms/create` с txHash
  5. Получить roomId и gameId
  6. Подключиться к Colyseus Room через `colyseusService.joinWithReservation()`
  7. Перейти в GameRoom с полученным roomId
- Обработка ошибок:
  - Отклонение транзакции пользователем
  - Недостаточный баланс
  - Ошибка при создании комнаты на backend
- Показывать статус: "Signing transaction..." → "Waiting for confirmation..." → "Creating room..." → "Joining game..."

#### 1.6 Frontend: Обновление RoomList для присоединения к комнате

**Изменения**:
- При клике на RoomCard:
  1. Проверить баланс пользователя
  2. Показать модальное окно подтверждения с информацией о ставке
  3. Вызвать TonTransactionService для транзакции присоединения
  4. Дождаться подтверждения
  5. Вызвать `POST /rooms/:roomId/join`
  6. Подключиться к Colyseus Room
  7. Перейти в GameRoom
- Обработка случая, когда комната уже заполнена
- Отображение статуса транзакции

#### 1.7 Mock-режим для разработки

**Создать**: Конфигурационный флаг `USE_MOCK_TRANSACTIONS`

**Логика**:
- Если флаг включен:
  - TonTransactionService возвращает mock txHash без реальной транзакции
  - Backend создает комнату без взаимодействия с контрактом
  - EscrowService использует фейковые gameId
  - Игра работает полностью на backend логике
- Если флаг выключен:
  - Полная интеграция с блокчейном
  - Реальные транзакции

**Цель**: Ускорить разработку и тестирование без необходимости каждый раз делать реальные транзакции

#### 1.8 Детальное логирование

**Добавить логи**:
- Все этапы создания транзакции
- Статус подтверждения в блокчейне
- Создание/обновление записи в БД
- Создание Colyseus Room
- WebSocket подключение
- Рендеринг GameRoom
- Синхронизация состояния игры

**Инструменты**: Winston на backend, console с timestamps на frontend

### Критерии завершения этапа
- ✅ Пользователь может создать комнату с реальной TON транзакцией
- ✅ Пользователь может создать комнату с RUBLE транзакцией
- ✅ Второй игрок может присоединиться к комнате с реальной транзакцией
- ✅ После присоединения оба игрока автоматически переходят в GameRoom
- ✅ GameRoom корректно отображает начальное состояние игры
- ✅ В БД корректно сохраняются все данные о комнате и транзакциях
- ✅ Mock-режим работает для локальной разработки
- ✅ Отсутствуют ошибки при переходе между экранами

---

## 🎲 ЭТАП 2: Завершение игрового процесса

### Цель этапа
Полностью работающая игра в длинные нарды с таймером, полным UI взаимодействием, валидацией ходов и определением победителя с автоматической выплатой.

### Задачи

#### 2.1 Frontend: Полное UI взаимодействие в GameRoom

**Обновить**: `GameRoom.jsx` и дочерние компоненты

**Функционал**:
- **Бросок костей**:
  - Кнопка "Roll Dice" активна только для текущего игрока
  - Анимация броска костей
  - Отображение выпавших значений
- **Отображение доступных ходов**:
  - Подсветка шашек, которыми можно ходить
  - Клик на шашку показывает возможные пункты назначения
  - Визуальные индикаторы (подсветка, стрелки)
- **Выполнение хода**:
  - Клик на подсвеченный пункт отправляет ход на сервер
  - Оптимистичное обновление UI (с откатом при ошибке)
  - Анимация перемещения шашки
- **Информация о текущем состоянии**:
  - Индикатор текущего игрока ("Your turn" / "Opponent's turn")
  - Количество ходов, которые нужно сделать
  - Количество снятых шашек
  - Количество шашек на баре
- **Обработка ошибок**:
  - Тосты при невалидном ходе
  - Сообщения от сервера об ошибках
- **Автоматическое завершение хода**:
  - Если нет доступных ходов, показывать кнопку "End Turn"
  - Автоматическое завершение, если dice массив пуст

#### 2.2 Backend: Таймер на ход

**Обновить**: `BackgammonRoom.ts`

**Логика**:
- При начале хода игрока запускать таймер на 60 секунд
- Каждую секунду обновлять `state.turnTimeLeft`
- Клиент видит обратный отсчет
- При достижении 0:
  - Автоматически завершить ход игрока
  - Если у игрока были доступные ходы - засчитать проигрыш
  - Передать ход оппоненту
- За 10 секунд до окончания - отправить предупреждение клиенту
- При выполнении хода игроком - сбросить таймер

**Добавить в GameState**:
- `turnTimeLeft`: number - оставшееся время в секундах
- `turnStartTime`: number - timestamp начала хода

#### 2.3 Backend: Обработка отключений игрока

**Логика**:
- При отключении игрока (`onLeave`):
  - Если игра еще не началась (waiting) - удалить комнату
  - Если игра идет (in_progress):
    - Установить таймер ожидания переподключения (30 секунд)
    - Если игрок не вернулся - засчитать ему поражение
    - Объявить победителем оппонента
- При переподключении (`allowReconnection: true`):
  - Восстановить состояние игры для игрока
  - Продолжить игру с текущего момента

**Добавить**:
- `reconnectionToken` для безопасного переподключения
- Логирование всех disconnects

#### 2.4 Backend: Завершение игры и вызов контракта

**Обновить**: `BackgammonRoom.ts` метод `declareWinner()`

**Логика**:
1. Установить `state.winner = winnerAddress`
2. Установить `state.phase = 'finished'`
3. Обновить таблицу `rooms` в БД:
   - `state = 'finished'`
   - `winner_address = winnerAddress`
4. Вызвать `EscrowService.reportWinner(gameId, winnerAddress)` от админа
5. Дождаться подтверждения транзакции
6. Сохранить `winner_tx_hash` в таблицу `rooms`
7. Обновить статистику игроков в таблице `users`:
   - Увеличить `games`, `wins`/`loses`, обновить `winrate`, `streaks`
8. Сохранить запись в таблицу `game_history`
9. Отправить клиентам сообщение `game_finished` с данными:
   - winner (address)
   - reason (normal_win, mars, timeout, disconnect, surrender)
   - txHash (хеш выплаты)
   - winnings (сумма выигрыша)
10. Заблокировать комнату (`lock()`)
11. Удалить комнату через 30 секунд

**Обработка ошибок**:
- Если транзакция ReportWinner не прошла:
  - Логировать ошибку
  - Повторить попытку (retry до 3 раз)
  - Уведомить админа
  - Оставить комнату в состоянии finished, но с флагом `payout_pending`

#### 2.5 Backend: Таблица game_history

**Создать таблицу**:
- `id` (UUID, PK)
- `game_id` (BIGINT) - ID из контракта
- `room_id` (UUID) - связь с таблицей rooms
- `player1_address` (VARCHAR)
- `player2_address` (VARCHAR)
- `winner_address` (VARCHAR)
- `loser_address` (VARCHAR)
- `currency` (VARCHAR) - TON/RUBLE
- `amount` (DECIMAL) - ставка
- `winnings` (DECIMAL) - сумма выигрыша
- `duration_seconds` (INT) - длительность игры
- `finish_reason` (VARCHAR) - normal_win, mars, timeout, disconnect, surrender
- `moves_count` (INT) - количество ходов
- `finished_at` (TIMESTAMP)
- `tx_hash` (VARCHAR) - хеш транзакции выплаты

**Миграция**: TypeORM миграция

#### 2.6 Frontend: Экран завершения игры

**Создать**: `components/GameFinished.jsx`

**Отображение**:
- Анимация победы/поражения
- Текст: "You Win!" / "You Lose"
- Информация о причине завершения
- Сумма выигрыша (для победителя)
- Хеш транзакции выплаты (ссылка на TON explorer)
- Статистика игры:
  - Длительность
  - Количество ходов
  - Обновленный winrate
- Кнопки:
  - "Back to Menu" - возврат в MainMenu
  - "Rematch" - создать новую комнату с теми же параметрами
  - "Claim Winnings" (только для RUBLE игр) - вызов ClaimJetton

**Для RUBLE игр**:
- Показать кнопку "Claim Winnings"
- При клике:
  - Вызвать `tonTransactionService.claimJettonWinnings(gameId)`
  - Показать статус транзакции
  - После подтверждения - обновить баланс
  - Кнопка становится неактивной после claim

#### 2.7 Frontend: Анимации и UX улучшения

**Добавить**:
- Плавные анимации перемещения шашек (CSS transitions / Framer Motion)
- Анимация броска костей (вращение)
- Подсветка доступных ходов (glow effect)
- Пульсация для индикации текущего игрока
- Звуковые эффекты (опционально):
  - Бросок костей
  - Перемещение шашки
  - Снятие шашки
  - Победа/поражение
- Тосты для уведомлений:
  - "Your turn"
  - "Invalid move"
  - "Opponent disconnected"
  - "10 seconds left"
- Скелетоны при загрузке GameRoom
- Индикатор загрузки при отправке хода

### Критерии завершения этапа
- ✅ Игроки видят корректную игровую доску с текущим состоянием
- ✅ Можно бросать кости и делать ходы по правилам длинных нард
- ✅ Доступные ходы подсвечиваются
- ✅ Анимации работают плавно
- ✅ Таймер работает, при таймауте игрок проигрывает
- ✅ Отключение игрока обрабатывается корректно
- ✅ Победитель определяется автоматически
- ✅ Backend вызывает ReportWinner в контракте
- ✅ Выигрыш начисляется победителю (автоматически для TON, через Claim для RUBLE)
- ✅ Игроки видят экран завершения игры
- ✅ Статистика обновляется в БД
- ✅ История игр сохраняется

---

## 📊 ЭТАП 3: Профиль, история и мониторинг

### Цель этапа
Добавить страницу профиля игрока, историю игр, мониторинг блокчейн событий и улучшить общий UX.

### Задачи

#### 3.1 Backend: Эндпоинты для статистики

**Создать**: `UsersController` методы

**Эндпоинты**:
- `GET /users/:address/stats` - статистика игрока:
  - games, wins, loses, winrate
  - streaks (current, best)
  - level, experience
  - total wagered/won (по валютам)
- `GET /users/:address/history` - история игр с пагинацией:
  - Query параметры: page, limit, currency (filter)
  - Возвращает массив игр из `game_history`
  - Сортировка по дате (новые первые)
- `GET /leaderboard` - топ игроков (опционально):
  - Сортировка по winrate
  - Минимум игр для участия (например, 10)
  - Limit 100

#### 3.2 Frontend: Страница профиля

**Создать**: `screens/Profile/Profile.jsx`

**Отображение**:
- Аватар и имя пользователя (из TON кошелька)
- Адрес кошелька (сокращенный, с копированием)
- Карточки статистики:
  - Winrate (процент, визуальный индикатор)
  - Игры / Победы / Поражения
  - Текущая серия / Лучшая серия
  - Level & Experience (progress bar)
- Статистика по валютам:
  - TON: Total Wagered, Total Won
  - RUBLE: Total Wagered, Total Won
- Таблица истории игр:
  - Колонки: Дата, Оппонент, Ставка (валюта), Результат (Win/Lose), Длительность
  - Пагинация (10 игр на страницу)
  - Фильтр по валюте (All / TON / RUBLE)
- Кнопка "Back to Menu"

**Навигация**:
- Добавить кнопку в MainMenu для перехода в профиль
- Или сделать клик на ProfileCard переходом в профиль

#### 3.3 Backend: Мониторинг контракта (ContractMonitorService)

**Создать**: `ContractMonitorService` в `ton/`

**Функционал**:
- Фоновый процесс (запускается при старте приложения)
- Каждые 10-15 секунд:
  - Получать последние транзакции контракта
  - Парсить события (CreateGameTon, JoinGameTon, ReportWinner, etc.)
  - Сверять с таблицей `rooms`:
    - Если создана игра, но нет записи в БД - логировать расхождение
    - Если игра завершена в контракте, но не в БД - обновить БД
- Сохранять все события в таблицу `blockchain_events`
- Отправлять алерты при критических расхождениях

**Таблица** `blockchain_events`:
- `id` (UUID, PK)
- `tx_hash` (VARCHAR, UNIQUE)
- `event_type` (VARCHAR) - create_game, join_game, report_winner, etc.
- `game_id` (BIGINT)
- `data` (JSONB) - полные данные события
- `timestamp` (TIMESTAMP)
- `processed` (BOOLEAN) - обработано ли событие

**Миграция**: TypeORM

#### 3.4 Frontend: UX улучшения

**Оптимизация производительности**:
- Мемоизация компонентов (React.memo) для Board, Point, Checker
- Оптимизация ре-рендеров (useMemo, useCallback)
- Lazy loading для Profile, GameRoom
- React Query для кэширования API запросов

**Улучшения интерфейса**:
- Скелетоны для загрузки:
  - ProfileCard (балансы)
  - RoomList
  - GameRoom (начальное состояние)
  - Profile (история игр)
- Оптимистичные обновления:
  - При создании комнаты - показывать ее сразу
  - При выполнении хода - анимировать до подтверждения сервера
- Toast notifications:
  - Успешное создание комнаты
  - Успешное присоединение
  - Ошибки транзакций
  - Завершение игры
- Модальные окна подтверждения:
  - Присоединение к комнате (показать информацию о ставке)
  - Выход из игры (forfeit)
  - Отмена создания комнаты
- Индикаторы загрузки:
  - Транзакция в процессе
  - Подключение к игре
  - Отправка хода

**Адаптивность**:
- Responsive дизайн для разных размеров экрана
- Оптимизация для мобильных устройств (touch-friendly)
- Поворот доски для второго игрока (опционально)

#### 3.5 Backend: Обработка edge cases

**Сценарии**:
- **Игрок создал комнату и ушел**:
  - Через 1 час (joinTimeout) комната автоматически отменяется
  - Создатель может вызвать `/rooms/:roomId/cancel` раньше
  - Рефанд через CancelGame в контракте
- **Игрок присоединился, но не подключился к Colyseus**:
  - Таймер ожидания 60 секунд
  - Если не подключился - засчитать ему проигрыш
  - Рефанд для второго игрока
- **Транзакция ReportWinner не прошла**:
  - Retry механизм (3 попытки с задержкой)
  - Логирование в отдельную таблицу `failed_transactions`
  - Алерт админу
  - Ручная обработка
- **Два игрока одновременно пытаются присоединиться к комнате**:
  - Race condition: первый, кто подтвердил транзакцию
  - Второму вернуть ошибку "Room already full"
  - Рефанд для второго
- **Расхождение состояния контракта и БД**:
  - ContractMonitorService обнаруживает
  - Автоматическая синхронизация (если возможно)
  - Алерт админу для ручной проверки

**Логирование**:
- Все критические операции логируются с Winston
- Уровни: error, warn, info, debug
- Логи сохраняются в файлы + stdout
- Ротация логов (по размеру/дате)

#### 3.6 Тестирование

**Backend тесты** (Jest):
- Unit тесты:
  - BackgammonLogic (расчет ходов, валидация)
  - EscrowService (mock TonClient)
  - RoomsService (mock TypeORM repository)
- Integration тесты:
  - API эндпоинты (create_room, join, stats)
  - Database операции
- E2E тесты:
  - Полный цикл создания и завершения игры (mock контракт)

**Frontend тесты** (Vitest):
- Unit тесты:
  - Компоненты (ProfileCard, RoomCard, CreateRoomModal)
  - Hooks (useWalletBalances)
  - Services (colyseusService, tonTransactionService)
- Snapshot тесты для основных компонентов

**Ручное тестирование**:
- Чеклист сценариев:
  - Создание комнаты (TON/RUBLE)
  - Присоединение к комнате
  - Полная игра до завершения
  - Таймаут хода
  - Отключение игрока
  - Отмена комнаты
  - Просмотр профиля и истории
- Тестирование на устройствах:
  - Desktop (Chrome, Firefox, Safari)
  - Mobile (iOS, Android)
  - Telegram WebApp (iOS, Android, Desktop)

### Критерии завершения этапа
- ✅ Профиль игрока показывает корректную статистику
- ✅ История игр сохраняется и отображается с пагинацией
- ✅ ContractMonitorService отслеживает события контракта
- ✅ Приложение работает плавно без лагов
- ✅ Все edge cases обработаны
- ✅ Логирование настроено
- ✅ Есть базовые тесты (покрытие > 50%)
- ✅ Приложение адаптировано под мобильные устройства
- ✅ UX улучшения реализованы (тосты, скелетоны, анимации)

---

## 📝 Приоритизация задач

### Критично (без этого игра не работает)
1. **ЭТАП 1**: Интеграция смарт-контракта, исправление ошибки перехода в игру
2. **ЭТАП 2**: Завершение игрового процесса (таймер, завершение игры, выплаты)

### Важно (улучшает UX и надежность)
3. **ЭТАП 3**: Профиль, история, мониторинг, UX улучшения

---

## 🔄 Дальнейшее развитие (после запуска)

### Возможные улучшения
- Турниры (соревнования с призовым фондом)
- Рейтинговая система (ELO)
- Чат в игре
- Реплеи игр (просмотр записи)
- Достижения и бейджи
- Реферальная система
- Поддержка других токенов
- Ставки с разными коэффициентами (марс = x2)
- Матчи на несколько раундов
- Приватные комнаты (по ссылке/паролю)
- Мобильные нативные приложения (iOS/Android)

---

## ✅ Чеклист готовности к запуску

### Функционал
- [ ] Пользователь может создать комнату с TON
- [ ] Пользователь может создать комнату с RUBLE
- [ ] Второй игрок может присоединиться к комнате
- [ ] Игра в нарды работает по правилам (длинные нарды)
- [ ] Таймер на ход работает корректно
- [ ] Победитель определяется автоматически
- [ ] Выигрыш начисляется через контракт (TON автоматически, RUBLE через Claim)
- [ ] Отключение/переподключение игрока обрабатывается
- [ ] Статистика сохраняется в БД
- [ ] История игр доступна в профиле

### Технические требования
- [ ] Backend работает стабильно без падений
- [ ] Frontend адаптирован под мобильные устройства
- [ ] Приложение работает в Telegram Mini App (iOS, Android, Desktop)
- [ ] Смарт-контракт задеплоен в mainnet
- [ ] База данных настроена с бэкапами
- [ ] Мониторинг и логирование настроены
- [ ] Безопасность проверена (HTTPS, rate limiting, validation)

### Документация
- [ ] README.md актуален
- [ ] DEPLOYMENT.md содержит инструкции по деплою
- [ ] API.md документирует все эндпоинты
- [ ] FAQ.md отвечает на частые вопросы пользователей

### Тестирование
- [ ] Ручное тестирование всех сценариев пройдено
- [ ] Тестирование на разных устройствах и браузерах
- [ ] Проведен soft launch с реальными пользователями
- [ ] Критические баги исправлены

---

## 📌 Заметки по разработке

### Работа с Cursor AI
- Каждый этап разбивается на подзадачи по файлам/модулям
- Используйте эту спецификацию как контекст для промптов
- Для каждой подзадачи указывайте конкретные файлы и требования
- Тестируйте каждый модуль отдельно перед интеграцией

### Git workflow
- Создавайте отдельные ветки для каждого этапа (feature/stage-1, feature/stage-2, etc.)
- Делайте коммиты после завершения логических блоков
- Используйте Pull Requests для ревью (даже при работе в одиночку)
- Теги для версий (v1.0.0-beta, v1.0.0, etc.)

### Тестирование
- После каждого этапа проводите регрессионное тестирование
- Используйте TON testnet для тестирования транзакций
- Создайте тестовые аккаунты с балансами TON и RUBLE
- Mock-режим для быстрой разработки без реальных транзакций

---

**Последнее обновление спецификации**: 17 ноября 2025  
**Версия документа**: 2.0 (актуализирована под текущую реализацию)
