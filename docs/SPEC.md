# 🎯 ROADMAP: Backgammon Online - TON Blockchain Game

## 📋 Общая информация о проекте

**Цель**: Запустить полнофункциональную онлайн-игру в длинные нарды со ставками в TON и RUBLE токенах.

**Текущее состояние**:
- ✅ SplashScreen + TonConnect авторизация
- ✅ MainMenu с балансами и списком комнат
- ✅ Backend (NestJS) + WebSocket (Colyseus)
- ✅ PostgreSQL подключена
- ✅ Escrow смарт-контракт готов к деплою
- ✅ Базовая логика игры написана (не протестирована)
- ✅ Частичный макет GameRoom

**Технологии**: NestJS, React, Vite, TON SDK, Colyseus, PostgreSQL, TypeScript

---

## 🚀 ЭТАП 1: Интеграция смарт-контракта и создание игровых комнат

### Цель этапа
Обеспечить полный цикл создания комнаты с реальной блокчейн-транзакцией и корректным переходом в игру.

### Задачи

#### 1.1 Backend: Интеграция с Escrow контрактом

**Файлы**: `backend/src/ton/`, `backend/src/transactions/`

- Создать сервис `EscrowService` для взаимодействия с контрактом
- Реализовать методы:
  - `deployContract()` - деплой контракта (если еще не задеплоен)
  - `createGameTon(amount, joinTimeout)` - отправка `CreateGameTon` (0x01)
  - `createGameJetton(jettonMaster, amount, joinTimeout)` - отправка Jetton с payload
  - `joinGameTon(gameId)` - отправка `JoinGameTon` (0x02)
  - `joinGameJetton(gameId, jettonMaster, amount)` - отправка Jetton с payload action=1
  - `reportWinner(gameId, winnerAddress)` - вызов `ReportWinner` (0x04) от админа
  - `listenToContractEvents()` - мониторинг транзакций контракта
- Добавить конфигурацию:
  ```typescript
  TON_ESCROW_ADDRESS=<адрес контракта>
  TON_ADMIN_MNEMONIC=<мнемоника админа>
  RUBLE_JETTON_MASTER=<адрес RUBLE токена>
  MIN_TON_BET=1
  MIN_RUBLE_BET=1000
  TON_FEE_PERCENT=5
  ```
- Валидация минимальных ставок на уровне API

#### 1.2 Backend: Модуль управления комнатами

**Файлы**: `backend/src/rooms/`, `backend/src/game/`

- Создать `RoomsModule` с эндпоинтами:
  - `POST /rooms/create` - создание комнаты
    - Принимает: `{ currency: 'TON' | 'RUBLE', amount: number }`
    - Валидирует баланс пользователя
    - Вызывает `EscrowService.createGame*()`
    - Сохраняет в БД: `rooms` таблица
    - Возвращает: `{ roomId, gameId, txHash }`
  - `POST /rooms/:roomId/join` - присоединение к комнате
    - Вызывает `EscrowService.joinGame*()`
    - Обновляет статус комнаты в БД
    - Инициирует создание Colyseus Room
  - `GET /rooms/available` - список доступных комнат
  - `GET /rooms/:roomId` - детали комнаты
  
- Создать схему БД `rooms`:
  ```sql
  CREATE TABLE rooms (
    id UUID PRIMARY KEY,
    game_id BIGINT UNIQUE NOT NULL, -- из контракта
    creator_address VARCHAR NOT NULL,
    joiner_address VARCHAR,
    currency VARCHAR(10) NOT NULL, -- 'TON' или 'RUBLE'
    amount DECIMAL NOT NULL,
    state VARCHAR(20) NOT NULL, -- 'waiting', 'in_progress', 'finished', 'cancelled'
    created_at TIMESTAMP DEFAULT NOW(),
    joined_at TIMESTAMP,
    winner_address VARCHAR,
    tx_hash VARCHAR, -- хеш транзакции создания
    colyseus_room_id VARCHAR -- ID комнаты Colyseus
  );
  ```

#### 1.3 Frontend: Создание комнаты с реальной транзакцией

**Файлы**: `frontend/src/components/MainMenu/`, `frontend/src/services/ton.ts`

- В компоненте `CreateRoomModal`:
  - Добавить выбор валюты (TON/RUBLE)
  - Валидация минимальных сумм на клиенте
  - При создании:
    1. Вызвать TonConnect для подписи транзакции
    2. Отправить транзакцию в контракт (через TonConnect)
    3. Дождаться подтверждения (polling или webhook)
    4. Вызвать `POST /rooms/create` с `txHash`
    5. Перейти в режим ожидания оппонента
  
- Создать `TonTransactionService`:
  ```typescript
  class TonTransactionService {
    async createGameTon(amount: number): Promise<string> // возвращает txHash
    async createGameRuble(amount: number): Promise<string>
    async joinGameTon(gameId: number, amount: number): Promise<string>
    async joinGameRuble(gameId: number, amount: number): Promise<string>
    async waitForConfirmation(txHash: string): Promise<boolean>
  }
  ```

#### 1.4 Frontend: Присоединение к комнате

**Файлы**: `frontend/src/components/MainMenu/RoomsList.tsx`

- При клике на комнату:
  1. Проверить баланс пользователя
  2. Вызвать TonConnect для транзакции присоединения
  3. Дождаться подтверждения
  4. Вызвать `POST /rooms/:roomId/join`
  5. Подключиться к Colyseus комнате
  6. Перейти в `GameRoom`

#### 1.5 Отладка перехода в игру

- Добавить детальное логирование на всех этапах:
  - Создание транзакции
  - Подтверждение в блокчейне
  - Создание записи в БД
  - Создание Colyseus Room
  - WebSocket подключение
  - Рендеринг GameRoom
- Создать mock-режим для тестирования без реальных транзакций (env: `USE_MOCK_TRANSACTIONS=true`)

### Критерии завершения этапа
- ✅ Пользователь может создать комнату с реальной TON транзакцией
- ✅ Пользователь может создать комнату с RUBLE транзакцией
- ✅ Второй игрок может присоединиться к комнате
- ✅ После присоединения оба игрока автоматически переходят в GameRoom
- ✅ В БД корректно сохраняются все данные о комнате и транзакциях

---

## 🎲 ЭТАП 2: Реализация игрового процесса (длинные нарды)

### Цель этапа
Полностью работающая игра в длинные нарды с таймером, валидацией ходов и определением победителя.

### Задачи

#### 2.1 Backend: Colyseus Room - игровая логика

**Файлы**: `backend/src/game/rooms/BackgammonRoom.ts`, `backend/src/game/logic/`

- Создать `BackgammonRoom extends Room`:
  - **State Schema**:
    ```typescript
    class BackgammonState extends Schema {
      @type("string") gameId: string;
      @type("string") currentPlayer: string; // адрес игрока
      @type([Point]) board: ArraySchema<Point>; // 24 пункта
      @type("number") whiteBar: number; // шашки на баре
      @type("number") blackBar: number;
      @type([number]) dice: ArraySchema<number>; // [кость1, кость2]
      @type("string") phase: string; // 'rolling', 'moving', 'waiting'
      @type("number") turnTimeLeft: number; // секунды
      @type("string") winner: string | null;
    }
    
    class Point extends Schema {
      @type("number") count: number; // количество шашек
      @type("string") color: string; // 'white', 'black', 'empty'
    }
    ```
  
  - **Методы**:
    - `onCreate(options)` - инициализация игры из БД
    - `onJoin(client, options)` - подключение игрока
    - `onMessage("roll_dice", handler)` - бросок костей
    - `onMessage("make_move", handler)` - совершение хода
    - `onMessage("end_turn", handler)` - завершение хода
    - `startTurnTimer()` - запуск таймера на 60 секунд
    - `onTurnTimeout()` - автоматический проигрыш
    - `checkWinCondition()` - проверка победы (все шашки выведены)
    - `onLeave(client)` - обработка отключения

- Создать `BackgammonLogic` сервис:
  ```typescript
  class BackgammonLogic {
    initializeBoard(): Point[] // начальная расстановка
    rollDice(): [number, number]
    getValidMoves(state, dice): Move[] // все возможные ходы
    validateMove(state, move): boolean
    applyMove(state, move): BackgammonState
    isGameFinished(state): boolean
    getWinner(state): string
  }
  ```

- **Правила длинных нард**:
  - 15 шашек у каждого игрока
  - Движение только в одном направлении
  - Нельзя ставить шашку на занятый пункт противника
  - Можно ставить на свои шашки
  - Вывод шашек начинается после сбора всех в дом
  - Обязательное использование всех бросков (если возможно)

#### 2.2 Backend: Таймер и автопроигрыш

**Файлы**: `backend/src/game/rooms/BackgammonRoom.ts`

- Реализовать систему таймера:
  ```typescript
  private turnTimer: NodeJS.Timeout;
  private turnStartTime: number;
  
  startTurnTimer() {
    this.turnStartTime = Date.now();
    this.state.turnTimeLeft = 60;
    
    // Обновление каждую секунду
    this.turnTimer = setInterval(() => {
      const elapsed = Math.floor((Date.now() - this.turnStartTime) / 1000);
      this.state.turnTimeLeft = Math.max(0, 60 - elapsed);
      
      if (this.state.turnTimeLeft === 0) {
        this.handleTimeout();
      }
    }, 1000);
  }
  
  handleTimeout() {
    clearInterval(this.turnTimer);
    const opponent = this.getOpponent(this.state.currentPlayer);
    this.declareWinner(opponent, 'timeout');
  }
  ```

#### 2.3 Backend: Завершение игры и вызов контракта

**Файлы**: `backend/src/game/rooms/BackgammonRoom.ts`, `backend/src/ton/escrow.service.ts`

- При определении победителя:
  ```typescript
  async declareWinner(winnerAddress: string, reason: string) {
    this.state.winner = winnerAddress;
    this.state.phase = 'finished';
    
    // Обновить БД
    await this.roomsService.updateRoom(this.state.gameId, {
      state: 'finished',
      winner_address: winnerAddress
    });
    
    // Вызвать контракт ReportWinner
    const txHash = await this.escrowService.reportWinner(
      this.state.gameId,
      winnerAddress
    );
    
    // Сохранить хеш транзакции
    await this.roomsService.updateRoom(this.state.gameId, {
      winner_tx_hash: txHash
    });
    
    // Уведомить клиентов
    this.broadcast("game_finished", {
      winner: winnerAddress,
      reason: reason,
      txHash: txHash
    });
    
    // Закрыть комнату через 10 секунд
    setTimeout(() => this.disconnect(), 10000);
  }
  ```

#### 2.4 Frontend: UI игровой доски

**Файлы**: `frontend/src/components/GameRoom/`, `frontend/src/components/Board/`

- Создать компоненты:
  - `Board.tsx` - игровая доска (24 пункта)
  - `Point.tsx` - один пункт доски с шашками
  - `Checker.tsx` - одна шашка
  - `Dice.tsx` - кости
  - `GameControls.tsx` - кнопки управления (бросить кости, завершить ход)
  - `TurnTimer.tsx` - таймер хода
  - `GameStatus.tsx` - текущий статус игры

- Визуализация:
  - Минималистичный дизайн в стиле приложения
  - Анимации перемещения шашек
  - Подсветка доступных ходов
  - Индикация текущего игрока
  - Отображение костей и оставшихся ходов

#### 2.5 Frontend: Логика взаимодействия с игрой

**Файлы**: `frontend/src/components/GameRoom/GameRoom.tsx`, `frontend/src/services/game.ts`

- Подключение к Colyseus:
  ```typescript
  useEffect(() => {
    const room = await client.joinById(roomId);
    
    room.onStateChange((state) => {
      setGameState(state);
    });
    
    room.onMessage("game_finished", (data) => {
      handleGameFinished(data);
    });
    
    return () => room.leave();
  }, [roomId]);
  ```

- Обработка действий игрока:
  - Клик "Бросить кости" → `room.send("roll_dice")`
  - Клик на шашку → показать доступные ходы
  - Клик на пункт назначения → `room.send("make_move", { from, to })`
  - Автоматическое завершение хода если нет доступных ходов
  - Отображение таймера с предупреждением на последних 10 секундах

#### 2.6 Frontend: Экран завершения игры

**Файлы**: `frontend/src/components/GameRoom/GameFinished.tsx`

- Показать результат:
  - Победитель / Проигравший
  - Причина завершения (победа, таймаут, отключение)
  - Сумма выигрыша (с учетом комиссии для TON)
  - Хеш транзакции выплаты
  - Кнопка "Вернуться в меню"
  - Кнопка "Реванш" (создать новую комнату с теми же параметрами)

- Для RUBLE игр:
  - Кнопка "Забрать выигрыш" → вызов `ClaimJetton` (0x11)
  - Показать статус claim транзакции

### Критерии завершения этапа
- ✅ Игроки видят корректную игровую доску
- ✅ Можно бросать кости и делать ходы по правилам длинных нард
- ✅ Таймер работает, при таймауте игрок проигрывает
- ✅ Победитель определяется автоматически
- ✅ Backend вызывает `ReportWinner` в контракте
- ✅ Выигрыш начисляется победителю (автоматически для TON, через Claim для RUBLE)
- ✅ Игроки видят результат игры и могут вернуться в меню

---

## 📊 ЭТАП 3: Статистика, история и оптимизация

### Цель этапа
Добавить профиль игрока, историю игр, winrate, оптимизировать производительность и UX.

### Задачи

#### 3.1 Backend: Модуль статистики

**Файлы**: `backend/src/users/`, `backend/src/statistics/`

- Расширить таблицу `users`:
  ```sql
  ALTER TABLE users ADD COLUMN games_played INT DEFAULT 0;
  ALTER TABLE users ADD COLUMN games_won INT DEFAULT 0;
  ALTER TABLE users ADD COLUMN total_wagered_ton DECIMAL DEFAULT 0;
  ALTER TABLE users ADD COLUMN total_wagered_ruble DECIMAL DEFAULT 0;
  ALTER TABLE users ADD COLUMN total_won_ton DECIMAL DEFAULT 0;
  ALTER TABLE users ADD COLUMN total_won_ruble DECIMAL DEFAULT 0;
  ```

- Создать таблицу `game_history`:
  ```sql
  CREATE TABLE game_history (
    id UUID PRIMARY KEY,
    game_id BIGINT NOT NULL,
    player1_address VARCHAR NOT NULL,
    player2_address VARCHAR NOT NULL,
    winner_address VARCHAR NOT NULL,
    currency VARCHAR(10) NOT NULL,
    amount DECIMAL NOT NULL,
    duration_seconds INT,
    finished_at TIMESTAMP DEFAULT NOW(),
    finish_reason VARCHAR(50) -- 'win', 'timeout', 'disconnect'
  );
  ```

- Создать эндпоинты:
  - `GET /users/:address/stats` - статистика игрока
  - `GET /users/:address/history` - история игр (пагинация)
  - `GET /leaderboard` - топ игроков по winrate (опционально)

- Автоматическое обновление статистики при завершении игры

#### 3.2 Frontend: Профиль игрока

**Файлы**: `frontend/src/components/Profile/`

- Создать страницу профиля (доступ через MainMenu):
  - Аватар и имя (из TON кошелька)
  - Адрес кошелька (сокращенный)
  - Winrate (процент побед)
  - Количество игр
  - Общая сумма ставок (TON/RUBLE)
  - Общий выигрыш (TON/RUBLE)

- История последних игр:
  - Таблица с колонками: Дата, Оппонент, Ставка, Результат
  - Пагинация (10 игр на страницу)
  - Фильтр по валюте

#### 3.3 Backend: Мониторинг контракта

**Файлы**: `backend/src/ton/contract-monitor.service.ts`

- Создать фоновый сервис для отслеживания событий контракта:
  ```typescript
  @Injectable()
  export class ContractMonitorService {
    async monitorTransactions() {
      // Каждые 10 секунд проверять новые транзакции
      // Обновлять статусы игр в БД
      // Логировать все события
    }
    
    async syncGameState(gameId: number) {
      // Получить состояние игры из контракта
      // Сверить с БД
      // Обновить если есть расхождения
    }
  }
  ```

- Логирование всех транзакций в отдельную таблицу `blockchain_events`

#### 3.4 Frontend: Оптимизация и UX улучшения

**Файлы**: весь frontend

- **Производительность**:
  - Мемоизация компонентов (React.memo)
  - Оптимизация ре-рендеров игровой доски
  - Lazy loading для тяжелых компонентов
  - Кэширование запросов к API (React Query)

- **UX улучшения**:
  - Скелетоны при загрузке данных
  - Оптимистичные обновления UI
  - Тосты для уведомлений (успех/ошибка)
  - Модальные окна подтверждения для важных действий
  - Индикаторы загрузки транзакций
  - Обработка ошибок с понятными сообщениями

- **Адаптивность**:
  - Оптимизация для мобильных устройств
  - Поворот доски для второго игрока
  - Touch-friendly элементы управления

#### 3.5 Backend: Обработка edge cases

**Файлы**: все модули backend

- **Обработка отключений**:
  - Если игрок отключился во время игры → автопроигрыш через 30 секунд
  - Если игрок не присоединился к созданной комнате → возможность отмены через `CancelGame`
  - Reconnect логика для кратковременных обрывов

- **Обработка ошибок транзакций**:
  - Retry логика для неудачных транзакций
  - Откат состояния БД при ошибке контракта
  - Уведомление пользователей о проблемах

- **Защита от читов**:
  - Все ходы валидируются на сервере
  - Невозможность изменить состояние игры с клиента
  - Логирование подозрительных действий

#### 3.6 Тестирование

**Файлы**: `backend/test/`, `frontend/src/__tests__/`

- **Backend тесты**:
  - Unit тесты для `BackgammonLogic`
  - Integration тесты для API эндпоинтов
  - E2E тесты для полного игрового цикла
  - Mock контракта для тестов

- **Frontend тесты**:
  - Unit тесты для компонентов
  - Тесты игровой логики
  - Snapshot тесты для UI

- **Ручное тестирование**:
  - Чеклист всех сценариев
  - Тестирование на разных устройствах
  - Стресс-тестирование (множество одновременных игр)

### Критерии завершения этапа
- ✅ Профиль игрока показывает корректную статистику
- ✅ История игр сохраняется и отображается
- ✅ Приложение работает плавно без лагов
- ✅ Все edge cases обработаны
- ✅ Покрытие тестами > 70%
- ✅ Приложение адаптировано под мобильные устройства

---

## 🚀 ЭТАП 4: Деплой и запуск

### Цель этапа
Развернуть приложение в продакшн, настроить мониторинг и подготовить к запуску.

### Задачи

#### 4.1 Инфраструктура

**Файлы**: `docker-compose.yml`, `Dockerfile`, `.env.production`

- **Backend деплой**:
  - Настроить Docker контейнеры
  - Настроить Nginx для reverse proxy
  - SSL сертификаты (Let's Encrypt)
  - Environment variables для продакшена
  - PM2 или аналог для process management

- **Frontend деплой**:
  - Build оптимизация (Vite production build)
  - CDN для статики (опционально)
  - Настройка Telegram Mini App манифеста

- **База данных**:
  - PostgreSQL в продакшене
  - Автоматические бэкапы
  - Миграции БД

#### 4.2 Деплой смарт-контракта

**Файлы**: `contracts/`, deployment scripts

- Деплой Escrow контракта в TON mainnet
- Верификация контракта
- Настройка admin кошелька
- Настройка fee wallet
- Тестовые транзакции для проверки

#### 4.3 Мониторинг и логирование

**Инструменты**: Winston, Sentry, Grafana (опционально)

- Настроить централизованное логирование:
  - Все ошибки backend
  - Все транзакции блокчейна
  - Метрики игр (количество, длительность)

- Алерты:
  - Ошибки при взаимодействии с контрактом
  - Падение backend сервиса
  - Критические ошибки в играх

- Дашборд с метриками:
  - Активные игры
  - Общий объем ставок
  - Количество пользователей
  - Средняя длительность игры

#### 4.4 Безопасность

**Файлы**: весь проект

- **Backend**:
  - Rate limiting на API
  - CORS настройки
  - Валидация всех входных данных
  - Защита от SQL injection
  - Секреты в environment variables

- **Frontend**:
  - Content Security Policy
  - XSS защита
  - Безопасное хранение данных (не хранить приватные ключи)

- **Смарт-контракт**:
  - Аудит кода (если возможно)
  - Тестирование на testnet
  - Ограничение прав admin кошелька

#### 4.5 Документация

**Файлы**: `README.md`, `DEPLOYMENT.md`, `API.md`

- Обновить README с:
  - Описанием проекта
  - Инструкцией по локальной разработке
  - Архитектурой системы

- Создать DEPLOYMENT.md:
  - Пошаговая инструкция деплоя
  - Настройка environment variables
  - Troubleshooting

- Создать API.md:
  - Документация всех эндпоинтов
  - Примеры запросов/ответов
  - WebSocket события

#### 4.6 Настройка Telegram Mini App

**Файлы**: `frontend/public/`, Telegram BotFather

- Настроить манифест приложения
- Загрузить иконки и скриншоты
- Настроить бота через BotFather:
  - Установить Web App URL
  - Настроить описание
  - Добавить команды бота (если нужны)

- Тестирование в Telegram:
  - Проверка на iOS
  - Проверка на Android
  - Проверка на Desktop

#### 4.7 Подготовка к запуску

- **Маркетинг**:
  - Анонс в канале (где раздается RUBLE)
  - Подготовка инструкций для пользователей
  - FAQ по игре

- **Поддержка**:
  - Канал для багрепортов
  - Мониторинг первых игр
  - Готовность к быстрым фиксам

- **Soft launch**:
  - Запуск для ограниченной аудитории
  - Сбор фидбека
  - Исправление критических багов
  - Полный запуск

### Критерии завершения этапа
- ✅ Backend развернут и стабильно работает
- ✅ Frontend доступен через Telegram Mini App
- ✅ Смарт-контракт задеплоен в mainnet
- ✅ Мониторинг настроен и работает
- ✅ Документация полная и актуальная
- ✅ Проведен soft launch
- ✅ Приложение готово к публичному запуску

---

## 📝 Дополнительные рекомендации

### Приоритизация
1. **Критично**: Этап 1 и 2 - без них игра не работает
2. **Важно**: Этап 3 - улучшает UX и надежность
3. **Необходимо**: Этап 4 - для продакшена

### Работа с Cursor
- Каждый этап можно разбить на подзадачи по файлам/модулям
- Используй эту спецификацию как контекст для промптов
- Для каждой подзадачи указывай конкретные файлы и требования
- Тестируй каждый модуль отдельно перед интеграцией

### Git workflow
- Создавай отдельные ветки для каждого этапа
- Делай коммиты после завершения логических блоков
- Используй Pull Requests для ревью (даже если работаешь один)

### Тестирование
- После каждого этапа проводи полное регрессионное тестирование
- Используй testnet TON для тестирования транзакций
- Создай тестовые аккаунты с балансами

---

## ✅ Чеклист готовности к запуску

- [ ] Пользователь может создать комнату с TON
- [ ] Пользователь может создать комнату с RUBLE
- [ ] Второй игрок может присоединиться
- [ ] Игра в нарды работает по правилам
- [ ] Таймер работает корректно
- [ ] Победитель определяется автоматически
- [ ] Выигрыш начисляется через контракт
- [ ] Статистика сохраняется
- [ ] История игр доступна
- [ ] Приложение работает в Telegram
- [ ] Мониторинг настроен
- [ ] Документация готова
- [ ] Проведено тестирование
